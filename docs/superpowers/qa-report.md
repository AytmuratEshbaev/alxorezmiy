# Phase 10 QA Report — alxorezmiy.uz Next.js migration

Date: 2026-05-16
Branch: `claude/sad-cray-a2f0f3` (55 commits ahead of `main`)

## 1. Build summary

- `rm -rf .next` clean
- `npm run typecheck` — pass (no output)
- `npm run lint` — pass, **0 warnings, 0 errors**
- `npm run build` — success

Route counts:

- Static (`○`): `/_not-found`, `/robots.txt`, `/sitemap.xml` (3)
- SSG with `generateStaticParams` (`●`): 11 route templates × 4 locales = **40 prerendered locale routes** plus `[locale]/news/[id]` (dynamic params via Firebase)
- Dynamic (`ƒ`): 10 admin routes + 2 API routes + middleware

First Load JS shared baseline: **105 kB** (54.2 kB + 48.5 kB + 2.12 kB).

Per-locale page sizes range 1.04 kB – 3.17 kB; First Load JS 110–128 kB for public pages. Admin pages 218–223 kB (heavier — Firebase client + admin UI). Middleware: 103 kB.

## 2. Smoke matrix

Dev server: `npm run dev` on `http://localhost:3000`.

### Public locale routes (40 / 40 → 200)

| Route | uz | ru | kk | en |
|---|---|---|---|---|
| `/` | 200 | 200 | 200 | 200 |
| `/about` | 200 | 200 | 200 | 200 |
| `/admission` | 200 | 200 | 200 | 200 |
| `/contact` | 200 | 200 | 200 | 200 |
| `/directions` | 200 | 200 | 200 | 200 |
| `/faq` | 200 | 200 | 200 | 200 |
| `/gallery` | 200 | 200 | 200 | 200 |
| `/teachers` | 200 | 200 | 200 | 200 |
| `/achievements` | 200 | 200 | 200 | 200 |
| `/news` | 200 | 200 | 200 | 200 |

### Special routes

| Route | Status | Note |
|---|---|---|
| `/sitemap.xml` | 200 | Dynamic |
| `/robots.txt` | 200 | Dynamic |
| `/manifest.json` | 200 | From `public/` |
| `/admin/login` | 500 | **Expected** — `auth/invalid-api-key`, no `NEXT_PUBLIC_FIREBASE_*` envs locally. Will pass once Vercel envs set. |
| `GET /api/imagekit-auth` | 500 | **Expected** — no `IMAGEKIT_PRIVATE_KEY` locally. 500 (not 404), so route is mounted. |
| `POST /api/send-message {}` | 400 | **Expected** — Zod validation reject. Route works. |

## 3. Lint

`✔ No ESLint warnings or errors` — 0 warnings, 0 errors across all touched files.

## 4. Lighthouse

Not run locally. `npx lighthouse@13.3.0` is installable but Chrome headless on this Windows sandbox is unreliable. Recommendation: run Lighthouse against the **Vercel preview** URL after first deploy — that is the realistic perf target (production build + edge CDN). The Acceptance Criteria target (Performance ≥ 90, SEO ≥ 95, A11y ≥ 95) should be validated there.

## 5. Final repo audit

Root after cleanup (`git rm -r css/` + `rm -rf admin/`):

```
.claude/                 docs/                package-lock.json    src/
.env.local.example       next-env.d.ts        package.json         tsconfig.json
.eslintrc.json           next.config.mjs      public/              tsconfig.tsbuildinfo
.gitignore               node_modules/        README.md
.next/                   .prettierignore      .prettierrc.json
```

Verified gone:
- ✅ No `*.html` at root
- ✅ No `js/`, `css/`, `lang/`, `admin/`, `api/`, `assets/` at root
- ✅ No `sw.js`, `vercel.json`, root `manifest.json`, root `sitemap.xml`, root `robots.txt`
- ✅ Service worker + manifest now live under `public/` (`manifest.json`, `sw.js`, `workbox-*.js` generated by next-pwa)
- ✅ Assets under `public/assets/`

## 6. Acceptance criteria checklist (Spec §17)

| # | Criterion | Status |
|---|---|---|
| 1 | `npm run build` xatosiz | ✅ PASS |
| 2 | `npm run dev` 4 locale × all pages 200 | ✅ PASS (40/40) |
| 3 | Lighthouse Perf≥90 / SEO≥95 / A11y≥95 | ⏸ DEFERRED to Vercel preview |
| 4 | Cmd+K search (news + teachers) | ✅ Built (SearchModal/Trigger present); manual UX verification recommended in preview |
| 5 | Theme toggle (no flash) | ✅ Built (ThemeContext + no-flash script in locale layout) |
| 6 | Language switcher URL/content | ✅ Built (next-intl middleware + LanguageSwitcher) |
| 7 | Admin login + AuthGuard | ⏸ Requires Vercel envs to validate end-to-end |
| 8 | Image upload → ImageKit | ⏸ Requires `IMAGEKIT_PRIVATE_KEY` env |
| 9 | Contact form → Resend | ⏸ Requires `RESEND_API_KEY` env (route + validation verified) |
| 10 | Service worker / offline | ✅ Built (next-pwa generated `sw.js` + `workbox-*.js`); validate in production build |
| 11 | Dynamic sitemap (Firebase news IDs) | ✅ Built (`src/app/sitemap.ts`) |
| 12 | Legacy URL redirects (`/news/abc` → `/uz/news/abc`) | ✅ `next.config.mjs` `redirects()` configured |

Items marked ⏸ depend on Vercel env vars and cannot be exercised end-to-end locally; the code paths are wired and the routes mount correctly (500/400 vs 404 confirms this).

## 7. Verdict

**Ready for Vercel deploy.** No code blockers. Deploy steps:

1. Push branch and open PR (or push to `main`).
2. Connect repo to Vercel.
3. Set env vars from `.env.local.example` in Vercel project settings.
4. Trigger deploy.
5. Post-deploy: re-run smoke matrix against preview URL, run Lighthouse, validate admin login + ImageKit + Resend end-to-end.
